
const SectionTitle = ({ title }) => {
    return (
        <div>
            <h1 className='lg:text-5xl text-3xl  text-white text-center'>{title}</h1>
        </div>
    );
};

export default SectionTitle;